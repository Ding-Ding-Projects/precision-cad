#!/bin/sh
set -eu
root=$(git rev-parse --show-toplevel)
while read -r local_ref local_oid remote_ref remote_oid; do
  case "$local_oid" in
    0000000000000000000000000000000000000000) continue ;;
  esac
  node "$root/scripts/check-public-source.mjs" "$local_oid"
done

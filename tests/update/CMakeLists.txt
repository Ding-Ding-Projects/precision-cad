cmake_minimum_required(VERSION 3.21)
get_filename_component(UPDATE_SOURCE_ROOT "${CMAKE_CURRENT_SOURCE_DIR}/../.." ABSOLUTE)
if(CMAKE_CURRENT_SOURCE_DIR STREQUAL CMAKE_SOURCE_DIR)
  project(precision_update_tests LANGUAGES CXX)
  enable_testing()
  find_package(Qt6 6.8 REQUIRED COMPONENTS Core Network Test)
  qt_standard_project_setup(REQUIRES 6.8)
  add_subdirectory("${UPDATE_SOURCE_ROOT}/src/update" update-library)
endif()
find_package(Qt6 6.8 REQUIRED COMPONENTS Core Network Test)
qt_add_executable(precision_update_tests tst_update_service.cpp)
target_link_libraries(precision_update_tests PRIVATE precision_update Qt6::Core Qt6::Network Qt6::Test)
add_test(NAME precision-update COMMAND precision_update_tests -o "${CMAKE_CURRENT_BINARY_DIR}/precision-update.txt,txt")
set_tests_properties(precision-update PROPERTIES ENVIRONMENT_MODIFICATION "PATH=path_list_prepend:$<TARGET_FILE_DIR:Qt6::Core>")
